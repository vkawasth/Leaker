I am using random entropy flow as a method to simulate AUs (nodes) triggering observable events (i.e. + sentiments, -ve sentiments etc..).

These values will come from functions operating on FinSets as algebraic operations. Finsets here are nodes in a region.

This is modeling stimulus into a complex organization that is about to act.

  
